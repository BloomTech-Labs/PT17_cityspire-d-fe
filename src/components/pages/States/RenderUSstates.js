import React from 'react';
import StatesList from '../../common/StatesList';
import { Layout } from 'antd';

// const { Footer } = Layout;

function RenderStatesList(props) {
  return (
    <Layout>
      <StatesList />
    </Layout>
  );
}
export default RenderStatesList;
